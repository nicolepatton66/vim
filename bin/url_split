#!/usr/bin/env perl

# decode and pretty print url parameters

use URI::Escape;

my $url_params;
while(<>) {
    chomp;
    s/^http[^?]+\?//;
    $url_params = $_;
}

foreach my $param (split /&/, $url_params) {
    print uri_unescape($param) . "\n";
}
