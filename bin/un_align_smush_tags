#!/usr/bin/env ruby

ARGF.each do |line|
  line.chomp!.sub!(/^(\s*)/, '')
  leader = $1
  line.gsub!(/\s+/, ' ')
  line.gsub!(/%> <%=/, '%>___<%=')
  line.gsub!(/> </, '><')
  line.gsub!(/%>___<%=/, '%> <%=')
  line.gsub!(/ ,/, ',')
  puts "#{leader}#{line}"
end
