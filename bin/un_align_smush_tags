#!/usr/bin/env ruby

ARGF.each do |line|
  line.chomp!
  line.gsub!(/^(\s*)/, '')
  leader = $1
  line.gsub!(/\s+/, ' ')
  line.gsub!(/%> <%=/, '%>_<%=')
  line.gsub!(/> </, '><')
  line.gsub!(/%>_<%=/, '%> <%=')
  puts "#{leader}#{line}"
end
