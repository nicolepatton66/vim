#!/usr/bin/env perl

while(<>) {
    chomp($line = $_);
    $line =~ s/(^\s*)//;
    $leader = $1;
    $line =~ s/\s+/ /g;
    $line =~ s/%> <%=/%>_<%=/g;
    $line =~ s/> </></g;
    $line =~ s/%>_<%=/%> <%=/g;
    print $leader . $line . "\n";
}

exit 0;
