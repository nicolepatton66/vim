#!/usr/bin/env ruby

ARGF.each do |line|
  line.chomp!
  line.sub!(/^(\s*)/, '')
  leader = $1
  line.gsub!(/\s+/, ' ')
  line.gsub!(/%> <%=/, '%>___<%=')
  line.gsub!(/> </, '><')
  line.gsub!(/%>___<%=/, '%> <%=')
  puts "#{leader}#{line}"
end
