#!/usr/bin/env ruby

ARGF.each do |line|
  line.chomp!
  line.sub!(/^(\s*)/, '')
  leader = $1
  line.gsub!(/\s+/, ' ')
  line.gsub!(/%> <%=/, '%>_<%=')
  line.gsub!(/> </, '><')
  line.gsub!(/%>_<%=/, '%> <%=')
  puts "#{leader}#{line}"
end
